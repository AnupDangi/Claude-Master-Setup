# claude-master-setup compatibility package

This package moved to `agent-master-setup`.

```bash
npx agent-master-setup@latest
```

Version `1.0.4` preserves the old command while delegating to Agent Master v1.1. Existing `.master/state/loop.json` and `.master/state/handoff.json` files are migrated automatically.
