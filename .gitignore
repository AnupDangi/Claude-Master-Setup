# Secrets & local env
.env
.env.*
!.env.example

# Harness local state (worktree-local, not shared — ADR-007)
.master/state/
/tmp/harness_*

# Legacy location (kept during migration; agents/settings in .claude/ are still committed)
.claude/state/

# Nested local worktree used for npm install testing (not part of the package)
/test-harness/

# Common build/deps (extend per stack on bootstrap)
node_modules/
dist/
build/
__pycache__/
.pytest_cache/
target/
*.log
.cursor/
*.tgz
