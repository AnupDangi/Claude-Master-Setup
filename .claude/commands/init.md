---
description: Initialize the universal Agent Master project contract
allowed-tools: Bash(node:*), Bash(git:*)
---

# Agent Master Init

Run:

```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/cli.js" init
```

Do not implement product changes during initialization.
